name: aiops
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12  # or 3.13 for latest
  - pip
  - pip:
      - -r requirements.txt

# Note: After creating the environment, run:
# python -m spacy download en_core_web_md
